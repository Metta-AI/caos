# test-stack's dependency (format `<path> <name>`, path relative to this dir):
# the flake-builder that builds this flake into an image — named as a LOCAL dep
# (mounted at DEEP-DEPS/flake-builder by deepening) rather than /std, so nothing
# here resolves a builtin by name.
../../std/flake-builder flake-builder
