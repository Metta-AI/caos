# run-test's dependency (format `<path> <name>`, path relative to this dir): the
# flake-builder that builds this flake into an image — named as a LOCAL dep
# (mounted at DEEP-DEPS/flake-builder by deepening), the only way anything here
# can name a builtin.
../../std/flake-builder flake-builder
