# llm-call's dependencies (format `<path> <name>`, path relative to this dir): the
# rustc worker factory that compiles it, and the llm-client code crate it links. rustc DEPENDS on the runner pool
# itself and curries the built binary onto it, so no tool names a runner.
# LOCAL deps (mounted under DEEP-DEPS/ by deepening) rather than /std, so std
# has no ambient self-reference.
../rustc rustc
../llm-client llm-client
