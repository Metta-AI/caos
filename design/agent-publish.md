# Branch publication

Publish the commit at a source gitlink directly from the server's bare Git
repository. The agent records the chosen commit and destination before pushing.

Publication has three layers, matching imports:

| Layer | Interface |
| --- | --- |
| Server | `POST /git/push {destination, commit, branch, expected}` |
| Worker command | `caos push-git <https-url> <commit> <branch> --expected=<oid\|absent>` |
| Agent tool | `publish_source(source_tree, repository, branch)` |

`commit` is an exact full hash H. `branch` names a branch under
`refs/heads/`; this operation does not delete refs or publish tags.
`expected` is required: a full old hash E, or JSON `null` meaning that the
branch must not exist. The CLI spells that case `absent`.

The inline agent tool reads the selected gitlink and observes the destination
branch. Before sending the push, it persists H, E, destination and branch in
the existing publication record, associated with the durable tool-call identity.
The publication key is fixed for that tool call, rather than regenerated by
each attempt. Concurrent attempts use the same recorded intent. A resumed call
never switches to a newer source commit or refreshes its lease. A later call
can publish newer edits. The receipt always identifies the exact commit that was published.

Keep destination arguments explicit. The agent can use import provenance to
choose a repository and the source path as a branch-name convention. Copying or
renaming a gitlink does not implicitly create, rename or delete a remote branch.

The endpoint:

1. Validates the HTTPS destination, commit, branch and required expected value.
2. Checks H is a code commit, rejecting conversation ancestry, reserved
   source-tree `.caos` content and unresolved merge markers at the tip.
   Stored commits already have complete history: ingestion verifies new objects
   before exposing them, and startup audits existing storage. Publication does
   not re-walk that object closure.
3. Reads the destination branch. If it already equals H, report convergence.
   If it differs from E, return a conflict with the observed hash.
4. When E exists, require E to be an ancestor of H. The first version preserves
   today's fast-forward-only publication policy. A missing local E requires
   importing and integrating that remote history before publishing.
5. Pushes exactly `H:refs/heads/<branch>` from the server's existing bare repo,
   using an explicit `--force-with-lease=refs/heads/<branch>:<E>` and disabling
   tag following. An empty lease enforces branch creation. The ancestry check
   prevents this lease mechanism from authorizing a history rewrite.
6. Returns the published commit and branch. On an ambiguous transport failure,
   read the branch: H means converged, another value means conflict, and E
   still being present means uncertain. An unchanged branch alone does not
   prove an earlier request cannot still finish.

Git enforces the lease at the receiver, closing the race after step 3.
Objects travel directly from the server to the destination without a source
checkout. Standard Git push negotiation handles existing remote history.

Use the same token-file option, sensitive header and repository-scoped
credential helper as imports. Tokens remain outside Git objects and receipts.
Reuse the existing Git command setup instead of adding another authentication
path. The server endpoint is an object/ref operation; the agent records its
outcome in the conversation. Publication does not change the source gitlink.

On conflict, import the remote head, integrate it, test, and make a new
publication call. Never silently replace E with the latest remote value.
On an uncertain outcome, inspect before continuing. A recorded successful
publication remains successful even if another writer later advances the branch.

Existing publication records remain readable. Local imports keep their TUI command.

See [Git push leases](https://git-scm.com/docs/git-push) for the receiver's checks.
