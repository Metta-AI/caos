[package]
name = "worker-common"
version = "0.1.0"
edition = "2021"

# Shared glue for the Rust workers: thin wrappers over the `caos` CLI plus the
# bits of file I/O every worker repeats.

[lib]
name = "worker_common"
path = "src/lib.rs"

[dependencies]
xattr = "1"
