//! The chat v3 protocol: naming, records, transitions and validation shared
//! by the host client and the workers so every process accepts exactly the
//! same conversation history (design/chat.md).

pub mod v3;
