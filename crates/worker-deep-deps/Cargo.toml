[package]
name = "worker-deep-deps"
version = "0.1.0"
edition = "2021"

# No dependencies: the worker is pure glue over the `caos` CLI plus std file I/O,
# so it builds to a tiny static binary that needs no shell or coreutils in the
# image.

[[bin]]
name = "worker-deep-deps"
path = "src/main.rs"
