# The test invokes both bases through hashes held in curry blobs, so their
# seeded build chain must be named explicitly (see design/caos-expr.md).
../../std/bash bash
../../std/run-and-update-ref run-and-update-ref
../../std/cargo cargo
../../std/flake-builder flake-builder
# The image this client test runs in, with the client-repo setup as its
# /worker: a dependency, not a harness.
../lib lib
