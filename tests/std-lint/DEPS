# What this test reaches for (format `<path> <name>`); deep-deps
# mounts each RECURSIVELY DEEPENED at DEEP-DEPS/<name>.
# The image this client test runs in, with the client-repo setup as its
# /worker: a dependency, not a harness.
../lib lib
