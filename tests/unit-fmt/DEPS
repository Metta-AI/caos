# What this test reaches for (format `<path> <name>`); deep-deps mounts each
# RECURSIVELY DEEPENED at DEEP-DEPS/<name>.
#
# cargo  the worker that runs it — named here, so naming it in the expression
#        resolves it to the BUILT image
# rust   everything cargo compiles: Cargo.toml, Cargo.lock, rust-toolchain.toml
#        and crates/, in one directory precisely so a package can declare it
../../std/bash bash
../../std/flake-builder flake-builder
../../std/cargo cargo
../../rust rust
