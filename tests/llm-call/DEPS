# What this test reaches for (format `<path> <name>`). deep-deps mounts each
# RECURSIVELY DEEPENED at DEEP-DEPS/<name>, so a std entry brings its own deps
# with it; only what an edge CANNOT carry is listed here — the git closure
# covers tree references and nothing else, and a curry's base is a BLOB holding
# an image hash, not a tree reference.
../../std/bash bash
../../std/llm-call llm-call
../../std/llm-stub llm-stub
../../std/cargo cargo
../../std/flake-builder flake-builder
../../std/llm-test llm-test
