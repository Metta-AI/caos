Throwaway probe: do Claude Code hooks fire in a cloud session?

Each hook in ../.claude/settings.json appends to a file in this directory. A
cloud session is asked to commit whatever appeared, so the answer comes back
through git rather than through the web UI.

Delete this branch when the question is answered.
